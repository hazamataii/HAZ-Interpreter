# playFile

# Syntax:

```
playFile("PathToFile")
```

# Description:

The `playFile()` function is used to load and execute another `.hazl` file. Put the path to the file

# Example:

```
playFile("日本語.hazl")
```